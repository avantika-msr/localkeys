/**
 * Types and schemas for the **LocalKeys cli** package.
 *
 * @module @localkeys/cli/types
 * @remarks Re-exports types from @localkeys/core for backward compatibility
 */

// Re-export all types from @localkeys/core
export * from '@localkeys/core/types';
